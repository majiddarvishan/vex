@PACKAGE_INIT@

# vex CMake configuration file

include(CMakeFindDependencyMacro)

# Add any required dependencies here
# Example:
# find_dependency(Threads REQUIRED)

# Include the targets file
include("${CMAKE_CURRENT_LIST_DIR}/vexTargets.cmake")

# Set variables for convenience
set(vex_VERSION @PROJECT_VERSION@)
set(vex_VERSION_MAJOR @PROJECT_VERSION_MAJOR@)
set(vex_VERSION_MINOR @PROJECT_VERSION_MINOR@)
set(vex_VERSION_PATCH @PROJECT_VERSION_PATCH@)

# Check that all required components are present
check_required_components(vex)

# Provide information about available modules
set(vex_BUILD_LOGGER @BUILD_LOGGER@)
set(vex_BUILD_THREADPOOL @BUILD_THREADPOOL@)
set(vex_BUILD_NETWORKING @BUILD_NETWORKING@)
set(vex_BUILD_OBJECT_POOL @BUILD_OBJECT_POOL@)
set(vex_BUILD_MONITORING @BUILD_MONITORING@)

if(@METRICS_MULTI_THREADED@)
    find_dependency(Threads)
endif()