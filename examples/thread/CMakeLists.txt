cmake_minimum_required(VERSION 3.16)
project(vex_example_thread_pool)

if(TARGET Vex::thread_pool)
    add_executable(example_thread_pool example_thread_pool.cpp)
    target_link_libraries(example_thread_pool PRIVATE Vex::thread_pool)
    target_include_directories(example_thread_pool PRIVATE ${CMAKE_SOURCE_DIR}/../../include)
else()
    message(STATUS "Vex::thread_pool target not found; skipping thread_pool example")
endif()
